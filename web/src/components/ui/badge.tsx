// Stub — to be replaced with shadcn-style component library
export function Badge({ children, variant, className }: any) {
  return <span className={`inline-flex items-center rounded border border-border px-1.5 py-0.5 text-[10px] font-medium ${className || ""}`}>{children}</span>;
}
